services:
  extracteur:
    build: .
    container_name: extracteur
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file: .env
    environment:
      STATUS_FILE: /app/data/status.json
    volumes:
      - ./data:/app/data
